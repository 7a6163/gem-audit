comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false

coverage:
  status:
    project:
      default:
        # The suite covers every line; do not let that slip.
        target: 100%
        threshold: 0%
    patch:
      default:
        target: 100%
